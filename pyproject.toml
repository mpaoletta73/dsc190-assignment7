[project]
name = "07-reproducibility"
version = "0.1.0"
description = "Reproducible Data Pipelines assignment"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dvc>=3.0.0",
    "marimo>=0.1.0",
    "pandas>=2.0.0",
    "matplotlib>=3.0.0",
]

[tool.uv]
package = false